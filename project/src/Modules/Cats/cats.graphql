type Cat {
  id: String!
  name: String!
  age: Int
  breed: String
}

type Query {
  cat(id: String!): Cat
  cats: [Cat]
}

type Mutation {
  createCat(name: String!, age: Int, breed: String): Cat
  deleteCat(id: String!): Cat
}
