# ../User/user.graphql
type Auth {
  accessToken: String
}
type Mutation {
  createToken(email: String!, password: String!): Auth
  createCaptcha(email: String!): Boolean
  modifyCaptchaStatus(email: String!, captcha: String!): Boolean
  signUp(email: String!, password: String!): User
}
